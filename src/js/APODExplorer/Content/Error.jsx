import React from 'react';

export default () => (
    <div className={"modal centered"}>
      <span className="labels error">
        Cannot show Picture of the day due to a server issue
      </span>
    </div>
);